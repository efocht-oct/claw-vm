# Example configuration for claw-vm scripts

# Where VM images/config live
VM_DIR="$HOME/ubuntu24-qemu-claw"

# VM identity
VM_NAME="noble-claw"

# VM resources
RAM_MB=8192
CPUS=4
DISK_GB=30

# Networking
BRIDGE=br0
SSH_FWD_PORT=2222

# VM user
VM_USER=claw

# SSH public key (optional; if unset, scripts try ~/.ssh/id_rsa.pub)
# SSH_PUBKEY="ssh-ed25519 AAAA..."
